import { Component, inject } from '@angular/core'; import { FormsModule } from '@angular/forms'; import { Router } from '@angular/router'; import { IonIcon } from '@ionic/angular/standalone'; import { UserRole } from '../../core/models/user-account.model'; import { MockSessionService } from '../../core/services/mock-session.service';
@Component({selector:'app-login',standalone:true,imports:[FormsModule,IonIcon],templateUrl:'./login.component.html'}) export class LoginComponent { private readonly session=inject(MockSessionService);private readonly router=inject(Router);role:UserRole='SUPER_ADMIN';email='hery.admin@example.test';password='demonstration';async login():Promise<void>{this.session.login(this.role);await this.router.navigateByUrl('/dashboard');}}
