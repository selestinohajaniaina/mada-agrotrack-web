import { Component, inject, signal } from '@angular/core'; import { FormsModule } from '@angular/forms'; import { IonIcon } from '@ionic/angular/standalone'; import { rolePermissions } from '../../core/models/user-account.model'; import { MockSessionService } from '../../core/services/mock-session.service';
@Component({selector:'app-profile',standalone:true,imports:[FormsModule,IonIcon],templateUrl:'./profile.component.html'}) export class ProfileComponent { readonly session=inject(MockSessionService); readonly notice=signal('');readonly rolePermissions=rolePermissions;currentPassword='';newPassword='';savePassword():void{this.currentPassword='';this.newPassword='';this.notice.set('Mot de passe simulé modifié dans l’interface');setTimeout(()=>this.notice.set(''),2500);}}
