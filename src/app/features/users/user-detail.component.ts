import { Component, inject } from '@angular/core'; import { ActivatedRoute, RouterLink } from '@angular/router'; import { IonIcon } from '@ionic/angular/standalone'; import { roleLabels, rolePermissions } from '../../core/models/user-account.model'; import { MockSessionService } from '../../core/services/mock-session.service'; import { UserStoreService } from '../../core/services/user-store.service'; import { StatusBadgeComponent } from '../../shared/components/status-badge/status-badge.component';
@Component({ selector:'app-user-detail', standalone:true, imports:[RouterLink,IonIcon,StatusBadgeComponent], templateUrl:'./user-detail.component.html' }) export class UserDetailComponent { readonly store=inject(UserStoreService); readonly session=inject(MockSessionService); readonly user=this.store.get(inject(ActivatedRoute).snapshot.paramMap.get('id')??''); readonly roleLabels=roleLabels; readonly rolePermissions=rolePermissions; }
